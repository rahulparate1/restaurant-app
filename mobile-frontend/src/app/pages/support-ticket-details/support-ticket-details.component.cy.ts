import { SupportTicketDetailsComponent } from './support-ticket-details.component'

describe('SupportTicketDetailsComponent', () => {
  it('should mount', () => {
    cy.mount(SupportTicketDetailsComponent)
  })
})